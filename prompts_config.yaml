# Prompts and Messages Configuration for CVCustomizer
# This file contains all system messages, prompts, and UI strings
# model: "gpt-3.5-turbo"
# API Configuration
api:
  model: "gpt-4.1-mini"
  temperature: 0.4
  max_tokens: 4096
  timeout: 30

# System Messages for LLM API calls
system_messages:
  summarize_experience: |
    Write response using resume language (passive third person) using similar language and style to the job description.
  
  customize_achievements: |
    Write the response using similar language and style to the job description. Provide the response and do not add any additional commentary or remarks



# Prompts for LLM API calls
prompts:
  summarize_experience: |
    Using my resume , write 5-7 sentences to summarize my professional experience, including only what’s relevant to the job description. Highlight my experience or skills that are relevant. Emphasis qualities that attracts and relevant to the hirer upfront.  Do not use the words "the candidate".
    Resume:
    {experience_text}
  
  customize_achievements: |  
    Rewrite each paragraph in Achievements as a resume bullet point, using similar language to what's written in the job description.  

    Job Description: {job_description}
    Company: {company_name}
    Job Title: {job_title}
    
    Original Achievements:
    {achievements_text}

# Error and warning messages
messages:
  warnings:
    no_api_key: "Warning: No API key provided. Set LLM_API_KEY environment variable or pass api_key parameter."
    no_api_key_available: "Warning: No API key available. Returning original content."
    unexpected_api_response: "Warning: Unexpected API response format. Returning original content."
    api_call_failed: "Warning: API call failed: {error}. Returning original content."
  
  errors:
    file_not_found: "CV template file '{file}' not found."
    yaml_parse_error: "Error parsing YAML file: {error}"
    job_description_file_error: "Pls provide filename to the job desscription. Unable to open the file:{file}."
  
  success:
    cv_saved: "Customized CV saved to: {output_file}"
    cv_customization_completed: "CV customization completed successfully!"
    output_file: "Output file: {output_file}"



# Output file naming
output_settings:
  timestamp_format: "%Y%m%d_%H%M%S"
  file_extension: ".docx"

# HTTP headers
http_headers:
  authorization: "Authorization"
  content_type: "Content-Type"
  bearer_prefix: "Bearer {api_key}"
  application_json: "application/json"

# API response parsing
api_response:
  choices_key: "choices"
  message_key: "message"
  content_key: "content"
  min_choices_length: 1


